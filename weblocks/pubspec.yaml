name: weblocks
description: A native, asynchronous, read-write lock that works across isolates and tabs.
version: 0.0.0-wip.1
repository: https://github.com/simolus3/goodies.dart

environment:
  sdk: ^3.9.0-0

dependencies:
  ffi: ^2.1.4
  hooks: ^0.20.0
  code_assets: ^0.19.0
  web: ^1.1.1
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0

hooks:
  user_defines:
    weblocks:
      local_build: true
